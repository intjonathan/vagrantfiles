/**
 * WARNING: This host definition is automatically generated by Puppet.
 * ANY MANUAL CHANGES TO IT WILL GET OVERWRITTEN!
 */

/**
 * A host definition. You can create your own configuration files
 * in the conf.d directory (e.g. one per host). By default all *.conf
 * files in this directory are included.
 *
 */

object Host "trustyicinga2client.local" {
      
  import "generic-host"
  display_name = "trustyicinga2client.local"
  address = "10.0.1.85"
  groups = [  "linux_servers",  "mysql_servers",  "postgres_servers",  "clients",  "smtp_servers",  "ssh_servers",  "http_servers", ]
  vars.distro = "Ubuntu"
  vars.os = "linux"
  vars.virtual_machine = "true"
}
