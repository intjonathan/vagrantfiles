Vagrant.configure("2") do |config|
  # All Vagrant configuration is done here. The most common configuration
  # options are documented and commented below. For a complete reference,
  # please see the online documentation at vagrantup.com.

  ####################
  # Snippets to use
  ####################

  #NFS folder sharing for Puppet modules and manifests
  #config.vm.synced_folder "modules", "/etc/puppet/modules", :nfs => true

  ####################
  ####################

  #vagrant-hostmanager plugin options
  config.hostmanager.enabled = true
  config.hostmanager.ignore_private_ip = false
  #update the /etc/hosts of the host OS
  config.hostmanager.manage_host = true
  #incorporate offline box's IPs into the /etc/hosts of boxes that are up/being brought up
  config.hostmanager.include_offline = true

#   #Puppet icingamaster and PuppetDB server
#   config.vm.define "icingamaster".to_sym do |icingamaster_config|
#     icingamaster_config.vm.box = "ubuntu-13.10-server-amd64"
#     icingamaster_config.vm.hostname = "icingamaster.local"
#     icingamaster_config.hostmanager.enabled = true
#     icingamaster_config.hostmanager.aliases = %w(icingamaster puppet master)
#     icingamaster_config.vm.network :private_network, ip: "10.0.1.79"
#     #set some machine-specific options	
#     icingamaster_config.vm.provider "virtualbox" do |v|
#       #change the amount of RAM 
#       v.customize ["modifyvm", :id, "--memory", "1024"]
#     end
#     #NFS folder sharing for Puppet modules and manifests
#     config.vm.synced_folder "/Users/nick/Documents/git/puppet-code/modules", "/etc/puppet/modules" #:nfs => true
#     config.vm.synced_folder "manifests/", "/etc/puppet/manifests" #:nfs => true
#     config.vm.synced_folder "/Users/nick/Documents/git/puppet-code/hieradata", "/etc/puppet/hieradata" #:nfs => true
#     #Puppet icingamaster provisioning script
#     #icingamaster_config.vm.provision :shell, :path => "../provisioning_scripts/ubuntu_saucy_puppet_master_script.sh"
#   end  

  #icingaserver server
  config.vm.define "icingaserver".to_sym do |icingaserver_config|
    icingaserver_config.vm.box = "centos-6.5-amd64"
    icingaserver_config.vm.hostname = "icingaserver.local"
    icingaserver_config.hostmanager.enabled = true
    icingaserver_config.hostmanager.aliases = %w(www.icingaserver.local monitor.icingaserver.local icingaserver)
    icingaserver_config.vm.network :private_network, ip: "10.0.1.80"
    #set some machine-specific options	
    icingaserver_config.vm.provider "virtualbox" do |v|
      #change the amount of RAM 
      v.customize ["modifyvm", :id, "--memory", "1024"]
    end
    #Puppet agent provisioning script
    #icingaserver_config.vm.provision :shell, :path => "../provisioning_scripts/ubuntu_saucy_puppet_agent_script.sh.sh"
  end

  #an icingaserver client
  config.vm.define "icingaclient1".to_sym do |icingaclient1_config|
    icingaclient1_config.vm.box = "centos-6.5-amd64"
    icingaclient1_config.vm.hostname = "icingaclient1.local"
    icingaclient1_config.hostmanager.enabled = true
    icingaclient1_config.vm.network :private_network, ip: "10.0.1.81"
    #set some machine-specific options	
    icingaclient1_config.vm.provider "virtualbox" do |v|
      #change the amount of RAM 
      v.customize ["modifyvm", :id, "--memory", "512"]
    end
    #Puppet agent provisioning script
    #icingaclient1_config.vm.provision :shell, :path => "../provisioning_scripts/el6_puppet_agent_script.sh"
  end

  #an icingaserver client
  config.vm.define "icingaclient2".to_sym do |icingaclient2_config|
    icingaclient2_config.vm.box = "ubuntu-13.10-server-amd64"
    icingaclient2_config.vm.hostname = "icingaclient2.local"
    icingaclient2_config.hostmanager.enabled = true
    icingaclient2_config.vm.network :private_network, ip: "10.0.1.82"
    icingaclient2_config.vm.provider "virtualbox" do |v| 	
      #change the amount of RAM 
      v.customize ["modifyvm", :id, "--memory", "512"]
    end
    #Puppet agent provisioning script
    #icingaclient2_config.vm.provision :shell, :path => "../provisioning_scripts/ubuntu_saucy_puppet_agent_script.sh.sh"
  end

  # #a mail server for icingaserver.local to send notifications to
  # config.vm.define "mail".to_sym do |mail_config|
  # 	mail_config.vm.box = "ubuntu-13.10-server-amd64"
  # 	mail_config.vm.hostname = "mail.local"
  # 	mail_config.hostmanager.enabled = true
  # 	mail_config.vm.network :private_network, ip: "10.0.1.84"
  # 	#set some machine-specific options	
  # 	mail_config.vm.provider "virtualbox" do |v|
  #        #change the amount of RAM 
  #         v.customize ["modifyvm", :id, "--memory", "512"]
  #     end
  #     #Puppet agent provisioning script
  # 	mail_config.vm.provision :shell, :path => "../provisioning_scripts/ubuntu_saucy_puppet_agent_script.sh.sh"
  # end
end